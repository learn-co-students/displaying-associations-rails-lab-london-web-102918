<h1><% @artist.name %></h1>
<ul>
  <% @artist.songs.each do |s|%>
      <li><%= link_to s.title, song_path(s) %></li>
  <% end %>
    <%= @artist.songs.count %>
</ul>
<%= link_to "All Artists", artists_path %>
